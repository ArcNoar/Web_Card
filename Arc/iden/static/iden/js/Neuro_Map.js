var Neuro_Container = document.getElementById('N_Content')

let limiter = 100
let current = 0
let word = 'Combo'

//Neuro_Container.innerHTML += `<div class="Neuron"'><span class="N_Val" id='N_Value'>${current} </span></div>`
//var Ne_Val = document.getElementById('N_Value')

//if (current < limiter) {
//    var CC = setInterval(() => Neuro_Container.innerHTML = `<div class="Neuron"><span class="N_Val">${word} X ${current} </span></div>`, 100)
//    var CI = setInterval(() => current++, 100)
//    var CCS = setInterval(() => clearInterval(CC), limiter * 100)
//    var CIS = setInterval(() => clearInterval(CI), limiter * 100)
//    console.log(current)
//} 


    

                                                      
                
    